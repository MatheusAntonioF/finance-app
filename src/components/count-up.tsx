'use client';
/**
 * This is an old library that does not have support to server components and next js hydration
 * so we need to import the library and then export it marking the component as client
 */
import CountUp from 'react-countup';
export { CountUp };
